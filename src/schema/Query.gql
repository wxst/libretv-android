type Query{
  version: String!
  mantainer: String!
  channels(length: Int!): [Channel]!
  countries: [Country]!
  subdivisions: [Subdivision]!
  regions: [Region]!
  blocklists: [Blocklist]!
  streams: [Stream]!
}
