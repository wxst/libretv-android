type Query{
  version: String!
  mantainer: String!
  channels(length: Int!): [Channel]!
  channel(id: String!): [Channel]!
  countries(length: Int!): [Country]!
  subdivisions: [Subdivision]!
  regions: [Region]!
  blocklists: [Blocklist]!
  streams(length: Int!): [Stream!]!
}
